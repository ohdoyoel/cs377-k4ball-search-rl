# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp

# ---------------------------------------------------------------------------
# Training / search outputs — regeneratable, never commit
# ---------------------------------------------------------------------------

# Run/output directories produced by the experiment scripts (default out_dirs)
runs/
**/runs/
experiments/runs*/
experiments/rm_data*/
exp_*/runs*/
**/tb_logs/
**/tensorboard/

# Model checkpoints & datasets (policy.zip, *_2M.zip, vsa_rm.pt, vsa_dataset.npz, ...)
*.zip
*.pt
*.pth
*.ckpt
*.npz
*.npy
*.parquet

# Per-run metadata & logs
*.log
*.pid
config.json
summary.json
eval_summary.json
training_curve.csv
rm_normalization.json
events.out.tfevents.*

# Re-include the report/slide PDFs in docs/ (not matched above, kept for safety)
!docs/*.pdf
